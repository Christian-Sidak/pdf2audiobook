# Remote vast.ai render box. Install into a venv created with
# --system-site-packages so the image's CUDA torch/torchaudio are reused —
# torch is deliberately ABSENT here (a pip torch would replace the CUDA build).
numpy>=1.26
soundfile>=0.12
faster-whisper>=1.0
pydantic>=2.0
rapidfuzz>=3.0
ruamel.yaml>=0.18
jsonschema>=4.0
click>=8.1.0
rich>=13.0.0
pymupdf>=1.24.0
silero-vad>=5.1
librosa>=0.10
resemblyzer>=0.1.3
# qwen_tts is installed from git in bootstrap.sh (matches README):
#   pip install "git+https://github.com/QwenLM/Qwen3-TTS.git"
# flash-attn: optional, installed by bootstrap only with VAST_FLASH_ATTN=1
